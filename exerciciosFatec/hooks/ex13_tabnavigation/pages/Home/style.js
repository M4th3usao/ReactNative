import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container:{
    flex: 1,
    padding: 16,
    alignItems: 'center',
    textAlign: 'center',
    justifyContent: 'center',
  }
})

export {styles};